services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile

  nginx:
    image: nginx:1.28-alpine
    container_name: deliki-nginx
    depends_on:
      - api
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - ./nginx/web:/usr/share/nginx/html:ro
